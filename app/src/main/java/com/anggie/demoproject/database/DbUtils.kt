package com.anggie.demoproject.database

const val DB_NAME = "currencyDatabase.db"

class DbUtils {
    companion object {
        const val KEY_NAME = "name"
        const val KEY_SYMBOL = "symbol"
    }
}